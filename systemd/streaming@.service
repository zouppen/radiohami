[Unit]
Description=Internet radio streaming for %i
After=pipewire.service
Requires=pipewire.service

[Service]
Type=simple
EnvironmentFile=%h/radiohami/icecast/%I.conf
ExecStart=%Y/../bin/stream_to_icecast
Restart=always
RestartSec=10s

[Install]
WantedBy=default.target
