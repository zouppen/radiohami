#!/bin/sh -eu

# Outputs pipewire output id number matching environment variable
# SOURCE.
get_pw_source() {
    pw-dump | jq -er 'map(.info.props | select(."node.name" == env.SOURCE) | ."object.serial")[0]'
}
